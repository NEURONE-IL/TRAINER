import { TipoEtapaPipe } from './tipo-etapa.pipe';

describe('TipoEtapaPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoEtapaPipe();
    expect(pipe).toBeTruthy();
  });
});
